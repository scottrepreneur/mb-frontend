import React from 'react'
import BadgeList from '../../components/BadgeList'

export default function Redeem({ initialCurrency, params }) {
  return <BadgeList initialCurrency={initialCurrency} params={params} />
}
